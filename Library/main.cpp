#include <iostream>
#include <vector>
#include "algor.h"


int main(){
    int arr[] = {9,7,8,2,0,1};
    BubbleSort(arr);
    for(auto arr : i){
        cout << i << " ";
    }
    // print_jack();
    // std::cout << Factorial(10) << "\n";
    // std::cout << fibonacci(10);
    // print();
    // std::cout << find_min(2,find_min(3,1));

    return 0;
}